import React from 'react';

const Reservation: React.FC = () => {
    return (
        <div className="container py-5">
            <h2>Reservation Page</h2>
            <p>Book a table (placeholder).</p>
        </div>
    );
};

export default Reservation;
